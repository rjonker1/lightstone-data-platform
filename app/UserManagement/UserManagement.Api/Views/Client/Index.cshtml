@using System
@using UserManagement.Api.Modules
@using UserManagement.Api.ViewModels
@using UserManagement.Domain.Dtos

@Html.Partial("Shared/PageHeading", new PageHeadingViewModel("Clients", ""))

<table id="client_table" class="table table-striped table-bordered" cellspacing="0" width="100%">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th></th>
        </tr>
    </thead>
</table>

<script>
    var table = $("#client_table").DataTable({
        dom: '<"table-toolbar">frtip',
        retrieve: true,
        ajax: "/Clients",
        columns: [
            { data: "id", visible: false },
            { data: "clientName" },
            { data: null, defaultContent: "<button class='btn btn-primary entity-edit' data-url='/Clients/'>Edit</button>" }
        ]
    });

    $("div.table-toolbar").html("<button class='btn btn-default entity-add' data-url='/Clients/Add'>Add</button>");
</script>
