using System;
using System.Collections.Generic;
using System.Linq.Expressions;

namespace Lace.Source.Lightstone.Repository
{
    public interface IReadOnlyRepository
    {
        IEnumerable<T> GetAll<T>();
        IEnumerable<T> FindWith<T>(Expression<Func<T, object>> expression);
        //T FindWithGuidId<T>(Guid id);
        //T FIndWithIntId<T>(int id);
        //IEnumerable<T> FindAllWhere<T>(string[] parameters);
    }
}
